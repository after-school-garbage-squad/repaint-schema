components:
  schemas:
    iBeaconID:
      description: UUID of u128 that iBeacon has
      type: integer
    HWID:
      description: Hardware ID of u10 held by iBeacon
      type: integer
    serviceUUID:
      description: Bluetooth variable length serviceUUID
      type: string
    visitorID:
      description: ID of visitor
      type: string
      format: uuid
    notificationID:
      description: Id for notification
      type: string
    imageID:
      description: Id referring to photos taken by participants at the photo spot
      type: string
      format: uuid
    spotID:
      description: ID of spot
      type: string
      format: uuid
    eventID:
      description: ID of event
      type: string
      format: uuid
