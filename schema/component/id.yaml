components:
  schemas:
    eventID:
      description: イベントのパブリックID(ulid)
      type: string
    eventImageID:
      description: イベントのデフォルト画像のID(ulid)
      type: string
    visitorImageID:
      description: 参加者が撮影した画像のID(ulid)
      type: string
    currentImageID:
      description: 参加者が現在設定している画像のID(ulid)
      type: string
    spotID:
      description: スポットのパブリックID(ulid)
      type: string
    visitorID:
      description: 参加者のパブリックID(ulid)
      type: string
    registrationID:
      type: string
      description: スマホのプッシュ通知用のID
    imageID:
      description: 画像のID(ulid)
      type: string
