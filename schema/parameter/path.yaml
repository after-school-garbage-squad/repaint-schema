components:
  parameters:
    eventID:
      in: path
      name: eventId
      description: イベントのパブリックID
      required: true
      schema:
        $ref: ~/component/id.yaml#/eventID
    visitorID:
      in: path
      name: visitorId
      description: 参加者のパブリックID
      required: true
      schema:
        $ref: ~/component/id.yaml#/visitorID
