get:
  summary: Check spot status by QR code
  description: Check spot status by QR code
  operationId: checkSpotByQRCode
  parameters:
    - $ref: ~/parameter/path.yaml#/eventID
    - $ref: ~/parameter/path.yaml#/spotID
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: ~/component/spot.yaml#/Spot
    400:
      description: Bad Request
    401:
      description: Unauthorized
