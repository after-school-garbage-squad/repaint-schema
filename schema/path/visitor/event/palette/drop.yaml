post:
  summary: paletteをdropする
  description: paletteをdropする
  operationId: dropPalette
  tags:
    - visitor
  parameters:
    - $ref: ~/parameter/path.yaml#/visitorID
    - $ref: ~/parameter/path.yaml#/eventID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - spotId
          properties:
            spotId:
              $ref: ~/component/id.yaml#/spotID
  responses:
    204:
      description: No Content
    400:
      description: Bad Request
