get:
  summary: Request QR code to be presented when taking a photo
  description: Request QR code to be presented when taking a photo
  operationId: getJoinEventQR
  parameters:
    - $ref: ~/parameter/path.yaml#/visitorID
    - $ref: ~/parameter/path.yaml#/eventID
  responses:
    200:
      description: OK
      content:
        image/png:
          schema:
            type: string
            format: binary
    400:
      description: Bad Request
