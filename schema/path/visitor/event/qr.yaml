get:
  summary: Request QR code for joining the event
  description: Request QR code for joining the event
  operationId: getJoinEventQR
  parameters:
    - $ref: ~/parameter/path.yaml#/visitorID
  responses:
    200:
      description: OK
      content:
        image/png:
          schema:
            type: string
            format: binary
    400:
      description: Bad Request
