post:
  summary: Spot detection and palette drop process
  description: Spot detection and palette drop process
  operationId: dropPalette
  parameters:
    - $ref: ~/parameter/path.yaml#/visitorID
    - $ref: ~/parameter/path.yaml#/eventID
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ~/component/beacon.yaml#/Beacon
  responses:
    204:
      description: No Content
    400:
      description: Bad Request
